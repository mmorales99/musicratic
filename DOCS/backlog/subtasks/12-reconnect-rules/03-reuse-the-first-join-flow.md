# Status

not-started

# Priority

medium

# Feature

Reconnect join flow reuse

# Description

Reuse the same join flow when a visitor reconnects after inactivity.

# Acceptance Criteria

- Reconnect uses the same path as the first join.
- The session receives the same type of join request.
- The reconnect behavior stays consistent with the docs.
