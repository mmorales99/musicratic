{
  "name": "musicratic-mobile-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright E2E tests for Musicratic Flutter web",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:install": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.42.0"
  }
}
